# Ported from From CAD to Shelf

`passes.py`, `generate.py`, `qa.py` and `consistency.py` are copied **unchanged** from
[meryboth/from-cad-to-shelf](https://github.com/meryboth/from-cad-to-shelf) at commit `64caef5fa9523d54a1e10159fbf7b4690f931b1f`,
together with `fixtures/lumen`, `fixtures/field16`, `products/lumen`, `products/field16` and `brands/meridian`.

`qa.check` is the **baseline gate** of Study A (see `research/PROTOCOL.md`). Do not edit these files:
new measurements live in their own modules, so the baseline stays exactly what was published.
